services:
  - type: web
    name: aetherboard-hardcore-online
    runtime: node
    region: ohio
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
