services:
  - type: web
    name: aetherboard-online
    runtime: node
    region: ohio
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
