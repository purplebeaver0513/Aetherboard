services:
  - type: web
    name: aetherboard-v7-online
    runtime: node
    region: ohio
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
